# RF reference cases

Checked-in results use independent standard-library Python analytical calculations. Fixture metadata records convention. TypeScript core never imports verifier. Equation coverage manifest prevents untested production equations.
